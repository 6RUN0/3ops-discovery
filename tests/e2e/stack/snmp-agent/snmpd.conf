# Listen on all interfaces, UDP 161.
agentAddress udp:161

# SNMPv3 user for authPriv (SHA auth, AES priv). The passphrases here are the
# e2e reference creds and MUST equal the snmp_auths.yaml fixture (Task 9).
createUser netmon SHA "netmon-auth-pass" AES "netmon-priv-pass"
rouser netmon authPriv

# Minimal system identity so IF-MIB / system group answer.
sysLocation e2e
sysContact  platform
